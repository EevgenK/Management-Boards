{
  "name": "management-boards",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm run build -w client && npm run build -w server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
